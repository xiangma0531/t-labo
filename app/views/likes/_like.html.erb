<% if current_user.already_liked?(source) %>
  いいね 
  <%= link_to source_likes_path(source_id: source.id, user_id: current_user.id), class: "like-button", method: :delete, remote: true do %>
    ★
  <% end %>
<% else %>
  いいね 
  <%= link_to source_likes_path(source_id: source.id), class: "like-button", method: :post, remote: true do %>
    ☆
  <% end %>
<% end %>