$('#user_course_id').html('<%= j(select_tag :user_course_id, options_for_select(@courses.pluck(:name, :id)), include_blank: '科目を選択してください') %>');
<%# $('#user_course_id').html('<%= j(options_for_select(@courses.pluck(:name, :id))) >'); %>